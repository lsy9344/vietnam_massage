---
baseline_commit: df6a808
---

# Story 2.4: D코스 마사지사2 필수 검증

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a 카운터,
I want D코스에서 마사지사2가 없으면 저장 또는 방문완료 처리가 차단되기를,
so that 2:1 코스의 담당자 누락으로 수당과 콜 인정이 틀어지지 않게 할 수 있다.

## Acceptance Criteria

1. **정책 기반 D코스 인식**
   - **Given** 코스 마스터에서 D코스가 `마사지사2 필요`로 설정되어 있다
   - **When** 카운터가 D코스 콜 행을 입력한다
   - **Then** 시스템은 해당 코스가 마사지사2 필수 대상임을 인식한다
   - **And** 코스명 문자열이 아니라 코스 고유 ID와 적용월의 `CoursePolicy.requiresSecondTherapist` 기준으로 판단한다.

2. **마사지사2 누락 저장 차단**
   - **Given** D코스 콜 행에 마사지사2가 비어 있다
   - **When** 카운터가 행을 저장하려 한다
   - **Then** 저장은 차단된다
   - **And** 마사지사2 필수 오류가 한국어로 표시된다.

3. **마사지사2 누락 방문완료 차단**
   - **Given** D코스 콜 행에 마사지사2가 비어 있다
   - **When** 카운터가 상태를 `방문완료` 또는 stable code `VISIT_COMPLETE`로 변경하려 한다
   - **Then** 방문완료 처리는 차단된다
   - **And** 결제금액, 수당, 귀케어 풀, 콜인정 계산은 완료 처리되지 않는다.

4. **오류 시각 표현**
   - **Given** D코스 콜 행에 마사지사2 오류가 표시된다
   - **When** 오류 UI가 렌더링된다
   - **Then** 마사지사2 셀에는 danger ring과 `!` 아이콘, 텍스트 메시지가 함께 표시된다
   - **And** 색상만으로 오류 의미를 전달하지 않는다.

5. **오류 접근성 연결**
   - **Given** D코스 마사지사2 오류가 발생한다
   - **When** 사용자가 키보드 또는 스크린리더로 셀에 접근한다
   - **Then** 마사지사2 셀은 `aria-invalid="true"`를 가진다
   - **And** 오류 메시지는 `aria-describedby`로 셀에 연결된다.

6. **오류 알림**
   - **Given** 카운터가 저장 또는 방문완료 차단을 트리거한다
   - **When** 오류 메시지가 나타난다
   - **Then** 오류 메시지는 `role="alert"` 또는 `aria-live="assertive"`로 알림된다
   - **And** 사용자는 어떤 필드를 수정해야 하는지 알 수 있다.

7. **비D코스 마사지사2 선택 사항**
   - **Given** A, B, C, E 코스 콜 행이 있다
   - **When** 마사지사2가 비어 있는 상태로 저장한다
   - **Then** 저장은 허용된다
   - **And** 마사지사2 수당은 0 또는 `—`로 표시된다.

8. **D코스 정상 저장과 계산**
   - **Given** D코스 콜 행에 마사지사1과 마사지사2가 모두 배정되어 있다
   - **When** 저장 또는 방문완료 처리를 한다
   - **Then** 저장이 허용된다
   - **And** 각 담당자 수당은 Story 2.3 계산 규칙에 따라 별도로 계산된다.

9. **검증**
   - **Given** 개발자가 D코스 검증을 테스트한다
   - **When** D코스 마사지사2 누락 저장 차단, 방문완료 차단, ARIA 연결, 비D코스 허용, D코스 정상 저장 테스트를 실행한다
   - **Then** 모든 테스트가 통과한다
   - **And** 검증은 UI 표시뿐 아니라 서버 domain service에서도 수행된다.

## Tasks / Subtasks

- [x] Task 1: calls domain service에 정책 기반 마사지사2 필수 검증을 추가한다 (AC: 1-3, 7-8)
  - [x] `src/modules/calls/service-call-service.ts`에 `assertSecondTherapistRequirement()` 또는 동등한 helper를 추가한다.
  - [x] helper는 `operatingMonth.monthKey`, `courseId`, 현재 적용되는 `CoursePolicy.requiresSecondTherapist`, `therapist2Id`를 기준으로 판단한다. 코스명, TV 표시명, 화면 label, `course.code === "D"` 문자열 분기만으로 판단하지 않는다.
  - [x] `assertActiveCourse()`가 이미 정책 존재를 검사하므로 중복 조회가 과해지지 않게 현재 정책을 반환하도록 리팩터링하거나, 같은 `effectiveForMonth()` predicate를 쓰는 최소 조회 helper를 둔다.
  - [x] 필수인데 `therapist2Id`가 비어 있으면 `ServiceCallDomainError`를 던진다. 권장 code: `D_COURSE_SECOND_THERAPIST_REQUIRED`; 권장 message: `D코스는 마사지사2 필수입니다. 마사지사2를 배정해야 저장됩니다.`
  - [x] `saveBasicServiceCallRow()`에서 신규 행 생성 또는 기존 행 update 전에 검증한다. 실패 시 `ServiceCall` 또는 `ServiceCallAssignment`가 부분 생성되면 안 된다.
  - [x] `autosaveServiceCallRow()`에서 `serviceCall.updateMany()`, assignment write, status history, audit log보다 먼저 검증한다. 실패 시 기존 row, status history, audit log가 변경되지 않아야 한다.
  - [x] `방문완료`와 `VISIT_COMPLETE` 전환도 같은 검증으로 차단한다. D코스는 상태와 무관하게 마사지사2 없이 저장되지 않는 것이 최종 정책이다.
  - [x] A/B/C/E 또는 `requiresSecondTherapist=false` 정책인 코스는 마사지사2 없이 저장 가능해야 한다.
  - [x] D코스에 마사지사1/마사지사2가 모두 있으면 기존 Story 2.3 계산이 그대로 실행되어 `therapist1Commission`, `therapist2Commission`, `opsCallCredit`가 서버 DTO로 반환되어야 한다.

- [x] Task 2: 기존 완료콜 계산과 downstream 집계가 invalid D코스를 포함하지 않게 방어한다 (AC: 3, 8)
  - [x] 정상 저장 경로는 invalid D코스를 만들 수 없어야 한다.
  - [x] 기존 데이터나 fixture에 이미 `requiresSecondTherapist=true` + `therapist2=null` + `방문완료` 행이 있어도 `listCompletedServiceCallCalculationsForDate()`가 이를 `calculated` 완료 집계로 노출하지 않게 방어한다.
  - [x] 필요하면 `ServiceCallCalculationStatus`에 `second_therapist_required` 같은 명시 상태를 추가한다. 조용히 0원 계산 또는 정상 계산으로 처리하지 않는다.
  - [x] `ServiceCall`에 derived amount columns를 추가하지 않는다. 월마감 스냅샷은 후속 closing stories 책임이다.

- [x] Task 3: Server Action 오류 응답을 마사지사2 field error로 매핑한다 (AC: 2, 5-6)
  - [x] `src/app/(erp)/calls/actions.ts`의 `mapActionError()`에서 `D_COURSE_SECOND_THERAPIST_REQUIRED`를 `fieldErrors: { therapist2Id: [message] }`, `formError`, `domainErrorCode`가 있는 `ActionResult<ServiceCallRowDto>`로 반환한다.
  - [x] 다른 `ServiceCallDomainError`는 기존 안전한 한국어 form error 동작을 유지한다.
  - [x] `requirePermission("call:write")`, `serviceCallInputSchema.safeParse()`, `serviceCallAutosaveInputSchema.safeParse()`, `revalidatePath("/calls")` 성공 후 호출 패턴은 보존한다.
  - [x] 실패한 autosave는 Story 2.2처럼 같은 draft payload를 유지하고 inline retry를 제공해야 한다.

- [x] Task 4: 콜 원장 grid에 accessible D코스 마사지사2 오류 UI를 구현한다 (AC: 4-6)
  - [x] `src/app/(erp)/calls/editable-call-grid.tsx`의 `SelectCell` 또는 마사지사2 셀 wrapper가 field-level error를 받을 수 있게 확장한다.
  - [x] 마사지사2 field error가 있으면 select에 `aria-invalid="true"`와 오류 메시지 id를 가리키는 `aria-describedby`를 설정한다.
  - [x] 오류 메시지는 `role="alert"` 또는 `aria-live="assertive"`를 사용하고, danger ring + `!` 아이콘 + 텍스트를 함께 렌더링한다.
  - [x] 오류 상태는 색상만으로 전달하지 않는다. danger foreground는 existing token/class를 사용하되 텍스트 대비가 충분해야 한다.
  - [x] AddRowForm도 `therapist2Id` field error를 렌더링해야 한다. 현재 AddRowForm은 마사지사1/2 아래에 `InlineError`가 없으므로 추가한다.
  - [x] Editable row autosave 실패 시 `result.fieldErrors?.therapist2Id`를 row state로 보관하고 마사지사2 셀에 연결한다. 저장 성공 시 해당 field error를 지운다.
  - [x] `ComputedCell`은 실패 draft를 성공 계산처럼 표시하지 않는 Story 2.3 동작을 유지한다.
  - [x] keyboard 흐름은 기존 blur commit, `Enter` blur, retry 버튼 동작을 깨지 않는다.

- [x] Task 5: unit tests를 Story 2.4 범위로 확장한다 (AC: 1-3, 7-9)
  - [x] `src/modules/calls/service-call-service.test.ts`의 memory Prisma fixture에 D코스 course/policy를 추가한다. 해당 policy는 `requiresSecondTherapist: true`, base price 3,200,000, `opsCallCredit: 1`이어야 한다.
  - [x] D코스 + 마사지사2 누락 신규 저장이 `D_COURSE_SECOND_THERAPIST_REQUIRED`로 reject되고 row/assignment가 생성되지 않는지 검증한다.
  - [x] 기존 row autosave에서 course를 D코스로 바꾸거나 status를 `VISIT_COMPLETE`/`방문완료`로 바꾸려 할 때 마사지사2 누락이면 reject되고 기존 row/status history/audit event가 변하지 않는지 검증한다.
  - [x] `requiresSecondTherapist=false` 코스는 마사지사2 없이 저장 가능하고 `not_completed` 또는 Story 2.3 계산 상태를 정상 반환하는지 검증한다.
  - [x] D코스 + 마사지사1/마사지사2 모두 배정 + 방문완료는 저장되고 두 수당이 별도 계산되는지 검증한다.
  - [x] existing invalid completed D-row를 fixture로 직접 주입할 수 있다면 downstream completed calculation helper가 해당 row를 집계하지 않는지 검증한다.

- [x] Task 6: E2E와 static validator를 추가한다 (AC: 2, 4-6, 8-9)
  - [x] `tests/e2e/story-2-4-d-course-second-therapist.spec.ts`를 추가한다.
  - [x] E2E seed는 별도 D코스 또는 기존 D코스를 사용하되, 적용월 정책의 `requiresSecondTherapist=true`가 확실해야 한다. 이름이나 화면 label에 의존하지 않는다.
  - [x] E2E는 D코스 + 마사지사2 빈칸 저장/방문완료 시 `D코스는 마사지사2 필수` 메시지, `저장 보류`, retry 유지, `aria-invalid`, `aria-describedby`, alert/live region을 검증한다.
  - [x] E2E는 A/B/C/E 또는 비필수 정책 코스가 마사지사2 없이 저장되는 경로와 D코스에 마사지사2를 배정하면 저장/계산되는 경로를 검증한다.
  - [x] `scripts/validate-story-2-4.mjs`를 추가하고 `package.json`의 `npm run lint`에서 Story 2.3 validator 뒤에 연결한다.
  - [x] static validator는 `D_COURSE_SECOND_THERAPIST_REQUIRED`, `requiresSecondTherapist`, `aria-invalid`, `aria-describedby`, `role="alert"` 또는 `aria-live="assertive"`, Story 2.4 E2E 파일, calls docs/project-context 갱신을 확인한다.

- [x] Task 7: 문서와 프로젝트 컨텍스트를 동기화한다 (AC: 1-9)
  - [x] `src/modules/calls/README.md`와 `docs/modules/calls.md`에 Story 2.4 계약을 추가한다.
  - [x] `_bmad-output/project-context.md`에 Story 2.4 확정 규칙을 추가한다: D코스 판단은 `CoursePolicy.requiresSecondTherapist`, 마사지사2 누락 저장/방문완료 차단, field error/ARIA 요구, invalid completed D-row 집계 제외.
  - [x] Story File List에 생성/수정 파일을 빠짐없이 기록한다.

## Dev Notes

### Source Documents Loaded

- `sprint_status`: `_bmad-output/implementation-artifacts/sprint-status.yaml`
- `epics`: `_bmad-output/planning-artifacts/epics.md`
- `prd`: `_bmad-output/planning-artifacts/prds/prd-vietnam_massage-2026-06-07/prd.md`
- `architecture`: `_bmad-output/planning-artifacts/architecture.md`
- `ux`: `_bmad-output/planning-artifacts/ux-designs/ux-vietnam_massage-2026-06-07/DESIGN.md`, `EXPERIENCE.md`, `review-accessibility.md`, `validation-report.md`
- Persistent project facts: `_bmad-output/project-context.md`
- Previous story: `_bmad-output/implementation-artifacts/2-3-방문완료-기준-결제-수당-콜인정-계산.md`
- Current implementation files read: `prisma/schema.prisma`, `src/modules/calls/service-call-schema.ts`, `src/modules/calls/service-call-service.ts`, `src/modules/calls/service-call-service.test.ts`, `src/app/(erp)/calls/actions.ts`, `src/app/(erp)/calls/editable-call-grid.tsx`, `src/modules/masters/course-service.ts`, `src/modules/masters/course-schema.ts`, `docs/modules/calls.md`, `src/modules/calls/README.md`, `scripts/validate-story-2-2.mjs`, `scripts/validate-story-2-3.mjs`, `package.json`.
- Latest technical checks: Next.js Server Actions/revalidation docs, React `useActionState` docs, W3C/WAI ARIA error techniques.

### Discovery Results

- Loaded `{epics_content}` from 1 whole file: `_bmad-output/planning-artifacts/epics.md`.
- Loaded `{prd_content}` from relevant PRD shard: `_bmad-output/planning-artifacts/prds/prd-vietnam_massage-2026-06-07/prd.md`.
- Loaded `{architecture_content}` from 1 whole file: `_bmad-output/planning-artifacts/architecture.md`.
- Loaded `{ux_content}` from UX shard directory: `_bmad-output/planning-artifacts/ux-designs/ux-vietnam_massage-2026-06-07/`.
- Loaded `{project_context}` from `_bmad-output/project-context.md`.

### Epic 2 Context

Epic 2 makes the call ledger the source of reservation/visit state, payment inputs, discount, assignments, daily expense, and completed-call calculations. Story 2.4 protects the Story 2.3 completed-call math from missing second-therapist assignments on the 2:1 course. [Source: `_bmad-output/planning-artifacts/epics.md` - Epic 2, Story 2.4]

PRD FR-14 is the direct source: D코스 service calls require 마사지사2; A/B/C/E keep 마사지사2 optional; when both therapists exist, commissions are calculated separately. FR-11/FR-12 remain active constraints: status changes are audited, and only completed rows contribute to money/pool/call credit. [Source: `_bmad-output/planning-artifacts/prds/prd-vietnam_massage-2026-06-07/prd.md` - FR-11, FR-12, FR-14]

The project-context already records the non-negotiable rule: D코스 2인 필수 검증 must use `Course.id` and `CoursePolicy.requiresSecondTherapist`, not course name text. [Source: `_bmad-output/project-context.md` - 기술 스택 및 버전, Story 1.8/2.3 rules]

### Architecture Guardrails

- Business validation belongs in `src/modules/calls/service-call-service.ts`; React components may display server errors but must not be the only enforcement point. [Source: `_bmad-output/planning-artifacts/architecture.md` - Domain service boundary]
- Server Actions remain thin adapters: authenticate/authorize, validate with Zod, call the domain service, map `ActionResult<T>`, and revalidate `/calls` only after success. [Source: `_bmad-output/planning-artifacts/architecture.md` - API & Communication Patterns]
- Use `ActionResult<T>` with `fieldErrors`, `formError`, and `domainErrorCode` for user-visible failures. D-course missing therapist should map to `therapist2Id`, not only a generic row message. [Source: `_bmad-output/planning-artifacts/architecture.md` - Action Response Format]
- Do not introduce a REST API, Redux/Zustand, a new form framework for the grid, or a schema migration for this story. The needed field already exists on `CoursePolicy`. [Source: `prisma/schema.prisma`; `_bmad-output/planning-artifacts/architecture.md` - Frontend Architecture]
- Audit logs are for persisted sensitive changes. A blocked autosave must not create `service_call.status_changed` or `service_call.row_changed`, because no source row change occurred. [Source: `_bmad-output/planning-artifacts/architecture.md` - Audit Event Patterns]
- Domain tests are mandatory before UI-dependent workflows because this rule affects payout correctness. [Source: `_bmad-output/planning-artifacts/architecture.md` - Test Placement; Enforcement Guidelines]

### Current Code State to Preserve

- `CoursePolicy.requiresSecondTherapist` already exists in Prisma with DB mapping `requires_second_therapist`; no migration is needed for the rule itself. [Source: `prisma/schema.prisma`]
- `defaultCourseSeeds` marks code `D` with `requiresSecondTherapist: true`; this is seed evidence, not the runtime validation key. Runtime logic still reads the active policy for the selected operating month. [Source: `src/modules/masters/course-schema.ts`]
- `saveBasicServiceCallRow()` currently validates active time slot, room, course, code values, and employees before creating/updating calls and assignment rows. Add D-course validation in this transaction before writes. [Source: `src/modules/calls/service-call-service.ts`]
- `autosaveServiceCallRow()` currently writes row update, assignments, status history, and audit logs in one transaction. D-course validation must happen before those writes so failed attempts preserve the existing row and histories. [Source: `src/modules/calls/service-call-service.ts`]
- `findServiceCallWithRelations()` already includes `operatingMonth`, `course.policies`, and active assignments; reuse that shape for validation/calculation where possible. [Source: `src/modules/calls/service-call-service.ts`]
- Story 2.3 introduced `calculateServiceCallCompletion()` and `listCompletedServiceCallCalculationsForDate()`. D-course validation must not move calculation into the UI or add derived amount columns. [Source: `src/modules/calls/service-call-service.ts`; Story 2.3 file]
- `EditableCallGrid` already keeps local draft state on autosave failure and avoids showing failed drafts as successful calculations. Preserve this behavior while adding field-level D-course error rendering. [Source: `src/app/(erp)/calls/editable-call-grid.tsx`]
- `SelectCell` currently has no error props, and AddRowForm does not render `InlineError` for `therapist2Id`. Story 2.4 must close that gap. [Source: `src/app/(erp)/calls/editable-call-grid.tsx`]

### Previous Story Intelligence

- Story 2.3 completed-call calculation accepts both legacy `방문완료` and stable `VISIT_COMPLETE`. Story 2.4 must block both when a required second therapist is missing. [Source: Story 2.3 file; `src/modules/calls/service-call-service.ts`]
- Story 2.3 static validator pattern checks service code, grid UI, E2E file, docs, project-context, and package lint wiring. Use the same envelope for `scripts/validate-story-2-4.mjs`. [Source: `scripts/validate-story-2-3.mjs`]
- Story 2.2 established the autosave failure contract: local draft stays on screen, row state becomes `저장 보류`, and retry uses the same draft. D-course validation failures are autosave failures and must follow the same contract. [Source: Story 2.3 previous-story intelligence; `src/app/(erp)/calls/editable-call-grid.tsx`]
- Current tests use an in-memory Prisma fixture. Extend it rather than introducing a new database-dependent unit test harness. [Source: `src/modules/calls/service-call-service.test.ts`]

### Git Intelligence

- Recent commits follow story-scoped implementation: service/schema/tests/grid/action/e2e/static-validator/docs/project-context. Continue that pattern. [Source: `git log --oneline -5`]
- Most recent relevant commit: `df6a808 feat(story-2.3): 방문완료 기준 결제/수당/콜인정 계산`, touching calls service, calls tests, grid, E2E, static validator, docs, package lint, and project-context. [Source: `git show --stat --name-only df6a808`]

### UX Requirements

- The call ledger is a keyboard-first desktop grid. D-course + empty 마사지사2 blocks save/visit completion and shows inline danger error. [Source: `_bmad-output/planning-artifacts/ux-designs/ux-vietnam_massage-2026-06-07/EXPERIENCE.md` - Component Patterns]
- D-course error UI requires danger ring, row/cell message, `!` icon, and text. Color alone is insufficient. [Source: `EXPERIENCE.md` - State Patterns; `DESIGN.md` - `grid-cell-error`]
- The 마사지사2 control must get `aria-invalid="true"` and `aria-describedby` pointing to the error node. The block message must use alert/live semantics. [Source: `EXPERIENCE.md` - Accessibility Floor; `review-accessibility.md`; `validation-report.md`]
- Computed readonly cells keep `bg-readonly`, readable foreground text, tabular numerals, and server-returned values only. [Source: `DESIGN.md` - `grid-cell-computed`; Story 2.3 implementation]

### Latest Technical Information

- Next.js Server Actions are still the correct mutation boundary for this story; after successful mutation they can call `revalidatePath("/calls")` to update route data. Source: https://nextjs.org/docs/app/building-your-application/data-fetching/server-actions-and-mutations
- React `useActionState` remains suitable for the AddRowForm path; it lets server action results update form state and is already used in the current component. Source: https://react.dev/reference/react/useActionState
- W3C WCAG ARIA technique ARIA21 recommends using `aria-invalid` to mark failed fields and associating the error message with the field, commonly via `aria-describedby`. Source: https://www.w3.org/WAI/WCAG21/Techniques/aria/ARIA21.html
- W3C WCAG ARIA technique ARIA19 covers `role="alert"`/live regions for identifying errors; alert containers should be present for reliable announcement. Source: https://w3c.github.io/wcag/techniques/aria/ARIA19

### Testing Requirements

- Unit tests must prove server-side enforcement; E2E/accessibility checks are not enough.
- Include both Korean legacy status `방문완료` and stable `VISIT_COMPLETE` where state-completion behavior matters.
- Verify failure has no side effects: no row create/update, no assignment change, no status history, no audit log.
- Verify success path still returns Story 2.3 calculated readonly values for D코스 with two therapists.
- Verify UI exposes `aria-invalid`, `aria-describedby`, visible text, icon cue, alert/live semantics, `저장 보류`, and retry.

### Project Structure Notes

- Update domain logic in `src/modules/calls/service-call-service.ts`.
- Keep input schema changes in `src/modules/calls/service-call-schema.ts` only if needed for typed field errors; the D-course rule is domain validation, not a static Zod-only rule.
- Keep Server Action mapping in `src/app/(erp)/calls/actions.ts`.
- Keep route-near grid behavior in `src/app/(erp)/calls/editable-call-grid.tsx`.
- Put Story 2.4 static validator in `scripts/validate-story-2-4.mjs` and E2E in `tests/e2e/story-2-4-d-course-second-therapist.spec.ts`.

### References

- `_bmad-output/planning-artifacts/epics.md` - Epic 2, Story 2.4
- `_bmad-output/planning-artifacts/prds/prd-vietnam_massage-2026-06-07/prd.md` - FR-11, FR-12, FR-14
- `_bmad-output/planning-artifacts/architecture.md` - API & Communication Patterns, Frontend Architecture, Action Response Format, Enforcement Guidelines
- `_bmad-output/planning-artifacts/ux-designs/ux-vietnam_massage-2026-06-07/EXPERIENCE.md` - Component Patterns, State Patterns, Accessibility Floor
- `_bmad-output/planning-artifacts/ux-designs/ux-vietnam_massage-2026-06-07/DESIGN.md` - `grid-cell-error`, `grid-cell-computed`
- `_bmad-output/planning-artifacts/ux-designs/ux-vietnam_massage-2026-06-07/review-accessibility.md` - D-course ARIA gap and contrast notes
- `_bmad-output/project-context.md` - D-course policy lookup, Story 2.3 calls calculation context
- `_bmad-output/implementation-artifacts/2-3-방문완료-기준-결제-수당-콜인정-계산.md` - previous story learnings
- `prisma/schema.prisma` - current `CoursePolicy`, `ServiceCall`, `ServiceCallAssignment` schema
- `src/modules/calls/service-call-service.ts` - current save/autosave/calculation implementation
- `src/app/(erp)/calls/editable-call-grid.tsx` - current grid draft/autosave/computed cell UI
- `src/modules/masters/course-schema.ts` - default D-course seed policy
- Next.js Server Actions docs: https://nextjs.org/docs/app/building-your-application/data-fetching/server-actions-and-mutations
- React `useActionState` docs: https://react.dev/reference/react/useActionState
- W3C ARIA21: https://www.w3.org/WAI/WCAG21/Techniques/aria/ARIA21.html
- W3C ARIA19: https://w3c.github.io/wcag/techniques/aria/ARIA19

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

### Completion Notes List

- Ultimate context engine analysis completed - comprehensive developer guide created.

### File List

- `package.json`
- `scripts/validate-story-2-4.mjs`
- `tests/e2e/story-2-4-d-course-second-therapist.spec.ts`
- `src/app/(erp)/calls/actions.ts`
- `src/app/(erp)/calls/editable-call-grid.tsx`
- `src/modules/calls/service-call-service.ts`
- `src/modules/calls/service-call-service.test.ts`
- `src/modules/calls/README.md`
- `docs/modules/calls.md`
- `_bmad-output/project-context.md`
- `_bmad-output/implementation-artifacts/tests/test-summary.md`

## Change Log

- 2026-06-09: Story 2.4 implementation completed for policy-based D-course second therapist validation, server action field errors, accessible grid errors, unit/E2E/static validation, docs, and project context.
- 2026-06-09: Senior review auto-fixes applied: E2E status selection now uses stable `VISIT_COMPLETE`, grid displays `second_therapist_required` calculation errors, and Story 2.4 static validator guards that UI state.

## Senior Developer Review (AI)

Reviewer: GPT-5 Codex  
Date: 2026-06-09

### Outcome

Approve after auto-fixes. No CRITICAL issues remain.

### Findings Fixed

- HIGH: E2E selected duplicated `방문완료` option by label, which could choose the legacy status instead of stable `VISIT_COMPLETE`; changed Story 2.4 E2E to select the stable value.
- MEDIUM: Existing invalid completed D-row with `second_therapist_required` had a domain calculation status but the grid status cell did not render the error text; added explicit UI rendering.
- MEDIUM: Story record was not synchronized with implementation status, completed tasks, changed files, and review notes; updated this story and sprint tracking.

### Validation Checklist

- Story file loaded from `_bmad-output/implementation-artifacts/2-4-d코스-마사지사2-필수-검증.md`.
- Story status updated to `done`.
- Epic/story ID resolved as `2.4`.
- Project context, planning artifacts, source files, tests, E2E, docs, and static validator reviewed.
- Official docs web fallback checked for Next.js Server Actions/revalidation, React `useActionState`, and W3C ARIA error semantics.
- Acceptance Criteria 1-9 cross-checked against domain service, Server Action, grid UI, tests, E2E, docs, and static validator.
- File List reviewed and updated.
- Code quality and security review completed on changed source files.
- Sprint status synced to `done`.
