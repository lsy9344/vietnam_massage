---
baseline_commit: 91f79cdafc8adbc9d78df5ca72f2a79f55ab6492
created_at: 2026-06-09T00:00:00+0900
discovery:
  epics: _bmad-output/planning-artifacts/epics.md
  architecture: _bmad-output/planning-artifacts/architecture.md
  prd: _bmad-output/planning-artifacts/prds/prd-vietnam_massage-2026-06-07/prd.md
  ux:
    - _bmad-output/planning-artifacts/ux-designs/ux-vietnam_massage-2026-06-07/DESIGN.md
    - _bmad-output/planning-artifacts/ux-designs/ux-vietnam_massage-2026-06-07/EXPERIENCE.md
    - _bmad-output/planning-artifacts/ux-designs/ux-vietnam_massage-2026-06-07/validation-report.md
    - _bmad-output/planning-artifacts/ux-designs/ux-vietnam_massage-2026-06-07/review-accessibility.md
  previous_story: _bmad-output/implementation-artifacts/3-2-첫-화면-실시간-객실-콜-현황.md
---

# Story 3.3: 객실 현황 화면과 웨이터 안내 문구

Status: done

## Story

As a 웨이터,
I want 객실별 상태, 남은 시간, 종료확인, 안내 문구를 읽기 전용으로 확인하기를,
so that 원장을 수정하지 않고도 청소, 입실 안내, 대기 판단을 빠르게 할 수 있다.

## Acceptance Criteria

1. **Given** 웨이터가 로그인한다  
   **When** ERP에 진입한다  
   **Then** 역할별 landing은 객실 현황 화면이다  
   **And** 콜 원장, 정산, 마스터 설정 메뉴는 표시되지 않는다.
2. **Given** 웨이터 또는 조회 전용 사용자가 객실 현황 화면에 접근한다  
   **When** 화면이 렌더링된다  
   **Then** 11개 객실 상태 카드가 표시된다  
   **And** 각 카드는 Story 3.1의 `RoomStatusDto`를 사용한다.
3. **Given** 객실 상태 카드가 표시된다  
   **When** 객실 상태가 `사용중`, `청소중`, `예약`, `종료확인`, `빈방` 중 하나다  
   **Then** 상태별 안내 문구가 함께 표시된다  
   **And** 안내 문구는 상태별로 구분된다.
4. **Given** 표시 상태가 `사용중`이다  
   **When** 카드가 표시된다  
   **Then** 남은분과 종료예정이 표시된다  
   **And** 남은분은 음수로 표시되지 않는다.
5. **Given** 표시 상태가 `종료확인`이다  
   **When** 카드가 표시된다  
   **Then** 웨이터가 행동할 수 있는 짧은 안내 문구가 표시된다  
   **And** 상태는 색상만이 아니라 라벨과 글리프로도 구분된다.
6. **Given** 활성 콜이 없는 객실이다  
   **When** 카드가 표시된다  
   **Then** 상태는 `빈방`으로 표시된다  
   **And** 즉시 안내 가능함을 알 수 있는 간결한 문구 또는 `-` 값이 표시된다.
7. **Given** 사용자가 객실 현황 화면에서 입력을 시도한다  
   **When** 객실 카드 또는 상태 정보를 클릭한다  
   **Then** 콜 원장 수정 기능은 제공되지 않는다  
   **And** 객실 현황은 읽기 전용 조회 화면으로 유지된다.
8. **Given** 객실 현황 데이터 갱신이 지연된다  
   **When** 마지막 갱신 이후 지연이 감지된다  
   **Then** 마지막 갱신 시각 또는 갱신 지연 상태가 표시된다  
   **And** 웨이터가 오래된 상태를 최신 상태로 오해하지 않게 한다.
9. **Given** 개발자가 객실 현황 화면을 검증한다  
   **When** 역할별 landing, 읽기 전용 권한, 안내 문구, 종료확인, 빈방, 지연 상태 테스트를 실행한다  
   **Then** 모든 테스트가 통과한다  
   **And** 객실 현황은 콜 원장이나 정산 데이터를 변경하지 않는다.

## Tasks / Subtasks

- [x] Task 1: `/rooms` placeholder를 웨이터 객실 현황 화면으로 교체한다 (AC: 2, 3, 4, 5, 6, 8, 9)
  - [x] `src/app/(erp)/rooms/page.tsx`에서 `ErpEmptyState`를 제거하고 `requireRouteAccess("/rooms")`는 유지한다.
  - [x] `/live/page.tsx`의 운영월 선택 규칙을 재사용한다: `listOperatingMonths()`, `selectedOperatingMonthFor()`, `clampDateToOperatingMonth()`.
  - [x] `Promise.all`로 `listRoomStatuses({ operatingMonthId, serviceDate })`를 조회한다. `/rooms`는 객실 현황 전용이므로 `getDailyCallLedgerSummary()` KPI를 복제하지 않는다.
  - [x] 운영월이 없으면 blank screen 대신 운영월 생성 안내를 보여준다. 관리자에게만 `/masters/operating-months` 링크를 노출한다.
  - [x] `src/app/(erp)/rooms/loading.tsx`를 추가해 11개 객실 카드 skeleton을 즉시 표시한다.
- [x] Task 2: 역할 landing과 sidebar 권한을 검증 가능한 상태로 유지한다 (AC: 1, 7, 9)
  - [x] `src/lib/authorization.ts`의 `waiter -> /rooms`, `read_only_viewer -> /rooms` landing을 보존하고 테스트한다.
  - [x] 웨이터 route prefix는 `/rooms`만 허용한다. 콜 원장, 정산, 월마감, 대시보드, 마스터 설정, 감사 로그 접근을 열지 않는다.
  - [x] `src/lib/navigation.ts`에서 웨이터 sidebar는 운영 현황 그룹의 `객실 현황`만 보이게 한다. 조회 전용은 기존처럼 `/rooms`, `/tv`, `/dashboard/today` 범위를 유지한다.
  - [x] `RoleAwareSidebar`는 권한 없는 메뉴를 disabled로 렌더링하지 않고 계속 숨긴다.
- [x] Task 3: 웨이터 안내 문구를 `RoomStatusDto` 기반으로 표시한다 (AC: 3, 5, 6, 9)
  - [x] `src/components/domain/room-status-card.tsx`를 재사용한다. 새 `/rooms` 전용 카드에서 `displayStatus`, `remainingMinutes`, `expectedEndAt`, `guidanceText`를 다시 계산하지 않는다.
  - [x] `ROOM_STATUS_GUIDANCE_TEXT`의 상태별 문구를 `/rooms`에서도 source of truth로 취급한다. 문구를 바꿔야 하면 `src/modules/rooms/room-status-service.ts` 상수와 관련 테스트를 같이 갱신한다.
  - [x] `종료확인`은 운영자가 행동할 수 있는 짧은 문구를 표시한다. `RoomStatusCard`의 현재 "결제·확인 필요"를 유지하거나, 웨이터 문맥상 더 적합하면 `guidanceText`를 "청소/입실 안내 대기" 계열로 갱신하되 Story 3.2 회귀를 깨지 않는다.
  - [x] `빈방`은 `즉시 가능` 또는 `ROOM_STATUS_GUIDANCE_TEXT["빈방"]`을 표시하고, 코스/담당/시간 값은 `-`로 읽기 쉽게 둔다.
- [x] Task 4: 읽기 전용 갱신 affordance를 `/rooms`에 추가한다 (AC: 7, 8, 9)
  - [x] 기존 `src/app/(erp)/live/live-refresh-controller.tsx`를 route-neutral component로 이동하거나 `/rooms`에서 안전하게 재사용 가능한 이름으로 추출한다. 권장 위치는 `src/components/domain/room-status-refresh-controller.tsx`.
  - [x] 자동 갱신은 Story 3.2와 같은 기본 15초 `router.refresh()` polling으로 시작한다. `@tanstack/react-query`를 추가하지 않는다.
  - [x] 마지막 갱신 시각, `갱신 중`, `갱신 지연`, 수동 `새로고침`을 표시한다.
  - [x] 갱신 중에도 직전 렌더 값이 사라지지 않게 하고, 실패/지연 상태를 무표시로 두지 않는다.
- [x] Task 5: `/rooms` 화면을 입력 없는 운영 현황으로 제한한다 (AC: 1, 7, 9)
  - [x] 콜 원장 grid, autosave, Server Action, 지출 panel, 정산/월마감 mutation UI를 `/rooms`에 포함하지 않는다.
  - [x] 카드 클릭은 편집 모달이나 원장 수정 affordance를 열지 않는다. 필요하면 클릭 가능한 요소 자체를 두지 않는다.
  - [x] 날짜/운영월 조회 form은 read-only 조회 조건 변경만 수행한다. write action을 추가하지 않는다.
  - [x] 웨이터가 `/calls`, `/settlements`, `/masters/*` direct route에 접근하면 `requireRouteAccess()`가 `/rooms`로 redirect하는 동작을 E2E로 검증한다.
- [x] Task 6: Story 3.3 테스트와 정적 검증을 추가한다 (AC: 1-9)
  - [x] `tests/e2e/story-3-3-rooms-waiter-guidance.spec.ts`를 추가한다. fixture prefix는 `story33_*` 또는 `E2E33-*`를 사용한다.
  - [x] E2E는 웨이터 로그인 후 `/rooms` landing, sidebar 메뉴 숨김, 11개 카드, 상태 라벨+글리프, 상태별 안내 문구, `사용중` 남은분/종료예정, `종료확인` 행동 문구, `빈방` 즉시 가능/`-`, 갱신 상태, 콜 원장 편집 UI 부재를 검증한다.
  - [x] E2E는 조회 전용 사용자의 `/rooms` 접근도 검증하고, 웨이터 direct route 차단(`/calls`, `/settlements`, `/masters/rooms`)을 검증한다.
  - [x] `scripts/validate-story-3-3.mjs`를 추가하고 `package.json`의 `npm run lint` validator chain에 연결한다.
  - [x] validator는 `/rooms` placeholder 제거, `requireRouteAccess("/rooms")`, `listRoomStatuses`, `RoomStatusCard`, refresh controller, loading skeleton, role landing/sidebar guard, E2E spec, docs/project-context 동기화를 확인한다.
  - [x] Playwright는 locator와 web assertion을 사용한다. `waitForTimeout()`은 사용하지 않는다.
- [x] Task 7: 문서와 project context를 동기화한다 (AC: 1-9)
  - [x] `_bmad-output/project-context.md`에 Story 3.3 확정 규칙을 추가한다: `/rooms`는 웨이터/read-only 객실 현황 landing, 읽기 전용, `RoomStatusDto`/`RoomStatusCard` 재사용, 안내 문구 source of truth, UI 계산 재구현 금지, 15초 갱신/지연 표시.
  - [x] `docs/modules/rooms.md`와 `src/modules/rooms/README.md`에 `/rooms` 객실 현황 downstream 화면과 웨이터 안내 문구 규칙을 추가한다.
  - [x] 필요하면 `docs/modules/calls.md`에는 `/rooms`가 콜 원장 데이터를 읽기만 하며 콜 mutation을 수행하지 않는 downstream 소비자임을 남긴다.
- [x] Task 8: 검증을 실행하고 결과를 기록한다 (AC: 9)
  - [x] `node scripts/validate-story-3-3.mjs`
  - [x] `npm run test:unit`
  - [x] `npm run lint`
  - [ ] `npm run test:e2e -- tests/e2e/story-3-3-rooms-waiter-guidance.spec.ts`
  - [x] dev server listen이 sandbox에서 `EPERM`으로 막히면 Story 3.1/3.2처럼 차단 조건과 대체 검증을 정확히 기록한다. 통과하지 않은 명령을 통과했다고 쓰지 않는다.

## Dev Notes

### Discovery Results

- `{epics_content}`: 단일 `_bmad-output/planning-artifacts/epics.md`에서 Epic 3 전체와 Story 3.3 AC를 로드했다.
- `{prd_content}`: `_bmad-output/planning-artifacts/prds/prd-vietnam_massage-2026-06-07/prd.md`에서 UJ-3, FR-16~FR-19, FR-34, 객실 현황/TV 조회 전용 요구를 로드했다.
- `{architecture_content}`: 단일 `_bmad-output/planning-artifacts/architecture.md`에서 App Router, domain service boundary, `rooms` DTO + polling route 구조, 권한/로딩/지연 상태 요구를 로드했다.
- `{ux_content}`: UX `DESIGN.md`, `EXPERIENCE.md`, `validation-report.md`, `review-accessibility.md`에서 상태 토큰, 웨이터 Flow 4, 안내 문구, 색상+라벨+글리프, 종료확인/reduced-motion, 갱신 지연 규칙을 로드했다.
- Persistent facts: `_bmad-output/project-context.md`를 로드했고 Story 3.1/3.2 확정 규칙이 포함되어 있다.

### Story Scope

- 이번 story는 `/rooms` 객실 현황 화면과 웨이터 안내 문구다. `/live` 첫 화면은 Story 3.2에서 완료됐고, `/tv` fullscreen route는 Story 3.4 범위다. [Source: `_bmad-output/planning-artifacts/epics.md` - Epic 3 Story 3.2~3.4]
- `/rooms`는 웨이터와 조회 전용 사용자가 보는 읽기 전용 운영 현황 화면이다. 콜 원장 입력, autosave, 지출 입력, 정산/마감 mutation을 넣지 않는다. [Source: `_bmad-output/planning-artifacts/epics.md` - Story 3.3 AC 7, 9]
- 객실 상태 계산 source of truth는 `src/modules/rooms/room-status-service.ts`의 `listRoomStatuses()`와 `RoomStatusDto`다. `/rooms` page/component는 활성 콜 선택, 자정 넘김, 종료확인, 남은분 계산을 직접 구현하지 않는다. [Source: `_bmad-output/project-context.md` - Story 3.1/3.2 rules]
- `/rooms`는 `/live`와 달리 오늘 KPI/매출 요약 화면이 아니다. 상태 카드와 안내 문구 중심으로 구현하고 Epic 6 대시보드 지표를 당겨오지 않는다. [Source: `_bmad-output/planning-artifacts/epics.md` - Epic 3 vs Epic 6]

### Current Source State: Files Read and Required Changes

- `src/app/(erp)/rooms/page.tsx`는 현재 `ErpEmptyState` placeholder만 렌더링한다. 이 story에서 실제 page로 교체해야 하며 `requireRouteAccess("/rooms")`는 보존한다.
- `src/app/(erp)/live/page.tsx`는 운영월/날짜 선택, `listRoomStatuses()`, `RoomStatusCard`, `LiveRefreshController`, 운영월 없음 fallback 패턴을 이미 구현했다. `/rooms`는 이 패턴을 재사용하되 KPI summary는 복제하지 않는다.
- `src/app/(erp)/live/loading.tsx`는 11개 카드 skeleton 패턴을 제공한다. `/rooms/loading.tsx`는 이 구조를 복사/간소화할 수 있다.
- `src/app/(erp)/live/live-refresh-controller.tsx`는 `router.refresh()`, 15초 polling, 45초 stale 판단, `마지막 갱신`/`갱신 중`/`갱신 지연`/`새로고침` UI를 제공한다. `/rooms` 재사용을 위해 domain/shared component로 이름을 바꾸는 것이 중복을 줄인다.
- `src/components/domain/room-status-card.tsx`는 `RoomStatusDto`를 props로 받고 `StatusBadge`, `guidanceText`, `remainingMinutes`, `expectedEndAt`, 담당자 표시를 렌더링한다. `/rooms`가 새 카드 계산을 만들면 중복이다.
- `src/components/domain/status-badge.tsx`는 `사용중`, `예약`, `청소중`, `종료확인`, `빈방`의 라벨+글리프+상태 토큰을 이미 지원한다.
- `src/app/globals.css`는 `status-attention` ring과 `prefers-reduced-motion` fallback을 이미 가진다. `/rooms`는 이 토큰을 재사용하고 raw status hex를 작은 텍스트 색으로 쓰지 않는다.
- `src/lib/authorization.ts`는 `waiter`와 `read_only_viewer` landing을 `/rooms`로 이미 설정했고, 웨이터 route prefix는 `/rooms`만 허용한다. 구현 중 이 계약을 넓히지 않는다.
- `src/lib/navigation.ts`는 웨이터에게 `객실 현황`만 보여주고 조회 전용에게 `객실 현황`, `TV 현황판`, `오늘 대시보드`를 보여준다. 권한 없는 메뉴 숨김 정책을 유지한다.
- `RoleAwareSidebar`는 role 기반 메뉴만 렌더링한다. disabled menu를 추가하지 않는다.

### Data and Calculation Rules

- `listRoomStatuses({ operatingMonthId, serviceDate })`는 모든 active room을 `sortOrder` 순서로 반환하고, 활성 콜이 없으면 `displayStatus="빈방"` DTO를 만든다. [Source: `src/modules/rooms/room-status-service.ts`]
- 활성 객실 점유 상태는 `예약`/`RESERVED`, `사용중`/`IN_USE`/`USING`, `청소중`/`CLEANING`이다. `방문완료`/`VISIT_COMPLETE`, `노쇼`/`NO_SHOW`, `취소`/`CANCELED`는 객실을 점유하지 않는다. [Source: `src/modules/rooms/room-status-service.ts`]
- `사용중`의 남은분이 0이면 `displayStatus="종료확인"`으로 바꾸되 `sourceCallStatus`는 원본 상태를 보존한다. UI에서 다시 판단하지 않는다. [Source: `_bmad-output/implementation-artifacts/3-1-객실-상태-dto와-최신-활성-콜-계산.md`]
- 상태별 안내 문구는 현재 `ROOM_STATUS_GUIDANCE_TEXT`에 있다: `예약`, `사용중`, `청소중`, `종료확인`, `빈방`. 문구가 업무적으로 바뀌면 이 상수와 service tests를 같이 바꿔야 한다. [Source: `src/modules/rooms/room-status-service.ts`]
- `remainingMinutes`는 `사용중`일 때만 값이 있고 음수로 반환되지 않는다. `예약`/`청소중`/`빈방`은 `null` 또는 `-` 표시가 가능하다. [Source: `src/modules/rooms/dtos.ts`; `src/components/domain/room-status-card.tsx`]

### Architecture Compliance

- `/rooms/page.tsx`는 App Router Server Component로 domain query를 수행하고 `requireRouteAccess("/rooms")`를 먼저 실행한다. Next.js App Router docs 기준 Server Components는 서버에서 ORM/database query를 수행할 수 있고 query logic을 client bundle에 넣지 않는다. [Source: Next.js Fetching Data docs, https://nextjs.org/docs/app/getting-started/fetching-data]
- Client Component는 browser-only refresh control에만 쓴다. `listRoomStatuses()` 호출, 권한 검사, Prisma 접근을 Client Component로 옮기지 않는다. [Source: Next.js Fetching Data docs, https://nextjs.org/docs/app/getting-started/fetching-data]
- `router.refresh()`는 현재 route를 새 server request로 갱신하고 Server Component를 다시 렌더링한다. Story 3.2의 polling 패턴을 유지한다. [Source: Next.js `useRouter` docs, https://nextjs.org/docs/app/api-reference/functions/use-router]
- `loading.tsx`는 route segment의 의미 있는 즉시 로딩 상태를 제공한다. `/rooms`도 blank screen 대신 skeleton을 둔다. [Source: Next.js Fetching Data docs, https://nextjs.org/docs/app/getting-started/fetching-data]
- 이번 story에서 Prisma migration, 새 table, broad REST API, React Query dependency는 필요 없다.

### UX and Accessibility Guardrails

- 웨이터 Flow 4는 로그인 후 객실 현황 landing, 11개 상태 카드, 남은시간+안내 문구 확인, `종료확인` 전환, 갱신 지연 표시를 요구한다. [Source: `_bmad-output/planning-artifacts/ux-designs/ux-vietnam_massage-2026-06-07/EXPERIENCE.md` - Flow 4]
- 상태는 색상만으로 전달하지 않는다. `StatusBadge`의 색상 + 텍스트 라벨 + 글리프를 항상 표시한다. [Source: `_bmad-output/planning-artifacts/ux-designs/ux-vietnam_massage-2026-06-07/DESIGN.md` - status badge]
- `종료확인` 텍스트 배지는 어두운 `status-complete-check` 계열을 사용하고, 밝은 orange는 glow/ring 같은 비텍스트 accent로만 쓴다. [Source: `_bmad-output/planning-artifacts/ux-designs/ux-vietnam_massage-2026-06-07/DESIGN.md` - status token split]
- `빈방`은 filled bronze badge가 아니라 outline/ghost 스타일이다. 흰 글자 on bronze를 만들지 않는다. [Source: `_bmad-output/planning-artifacts/ux-designs/ux-vietnam_massage-2026-06-07/DESIGN.md` - 빈방 outline]
- `종료확인` attention은 `prefers-reduced-motion: reduce`에서 정적 ring + 글리프 fallback이어야 한다. 현재 `globals.css`에 motion reduction fallback이 있다. [Source: `src/app/globals.css`; `_bmad-output/planning-artifacts/ux-designs/ux-vietnam_massage-2026-06-07/EXPERIENCE.md`]
- 문구는 운영자가 바로 행동하는 짧은 한국어 라벨이어야 한다. 장황한 설명문이나 기능 사용 안내를 카드 안에 넣지 않는다. [Source: `_bmad-output/planning-artifacts/ux-designs/ux-vietnam_massage-2026-06-07/EXPERIENCE.md` - microcopy]

### Previous Story Intelligence

- Story 3.2 완료 커밋 `91f79cd`는 `/live`, `RoomStatusCard`, `LiveRefreshController`, `/live/loading.tsx`, `getDailyCallLedgerSummary()` 확장, Story 3.2 validator/E2E/docs sync를 만들었다. Story 3.3은 `/rooms`에 같은 room status surface를 재사용하는 story다.
- Story 3.2에서 `RoomStatusCard`가 `data-testid="room-status-card"`를 제공하므로 Story 3.3 E2E는 11개 카드 count와 카드별 상태/guidance를 안정적으로 검증할 수 있다.
- Story 3.2는 React Query를 도입하지 않고 `router.refresh()` polling을 사용했다. Story 3.3도 같은 패턴을 유지해 dependency drift를 막는다.
- 이전 story들은 story-specific validator를 `npm run lint` chain에 연결했다. Story 3.3도 validator 없이 ready 완료로 넘기면 이후 dev/review에서 누락이 커진다.
- Story 3.1/3.2 E2E는 dev server listen이 sandbox에서 `EPERM`으로 막힐 수 있음을 기록했다. 실행 불가 시 정확히 기록하고, 통과하지 않은 명령을 통과했다고 쓰지 않는다.

### Git Intelligence

- 최근 커밋 순서: `91f79cd feat(story-3.2)`, `995779f feat(story-3.1)`, `52bdc2c feat(story-2.6)`, `7645ff6 feat(story-2.5)`, `5f153d8 feat(story-2.4)`.
- 현재 작업 전 `git status --short`에는 `_bmad-output/story-automator/orchestration-1-20260607-165702.md`의 unrelated local modification이 있었다. 이 story 생성/구현에서 해당 파일을 되돌리거나 수정하지 않는다.
- Story 3.x 패턴은 source 변경 + docs/project-context sync + validator + E2E spec + lint chain 연결이다.

### Latest Technical Information

- Project-pinned versions: `next@16.2.7`, `react@19.2.7`, `react-dom@19.2.7`, `typescript@5.9.3`, `@prisma/client@7.8.0`, `prisma@7.8.0`, `@tanstack/react-table@8.21.3`, `@playwright/test@1.60.0`. 이번 story에서 dependency upgrade는 필요 없다. [Source: `package.json`]
- Next.js App Router `router.refresh()`는 현재 route를 새로 요청해 Server Components를 re-render한다. 기존 client/browser state 일부는 유지될 수 있으므로 refresh controller에는 pending/stale 표시를 명시적으로 둔다. [Source: https://nextjs.org/docs/app/api-reference/functions/use-router]
- Next.js `loading.tsx`는 route segment에 즉시 fallback UI를 제공한다. `/rooms/loading.tsx`는 11개 카드 skeleton으로 사용자가 응답 중임을 알게 해야 한다. [Source: https://nextjs.org/docs/app/getting-started/fetching-data]
- Playwright는 locator와 auto-waiting web assertion을 권장한다. E2E는 role/text/test id 기반 locator를 사용하고 hardcoded wait를 피한다. [Source: https://playwright.dev/docs/locators; https://playwright.dev/docs/test-assertions]

### Testing Requirements

- Authorization tests:
  - 웨이터 로그인 후 `/rooms` landing.
  - 웨이터 sidebar에 `객실 현황`은 보이고 `콜/예약 입력 원장`, `정산 화면`, `월마감`, `마스터 설정`, `감사 로그`는 보이지 않음.
  - 웨이터가 `/calls`, `/settlements`, `/masters/rooms` direct route로 가면 `/rooms`로 redirect.
  - 조회 전용 사용자는 `/rooms` 접근 가능.
- Room card behavior tests:
  - 11개 active room card 표시.
  - `예약`, `사용중`, `청소중`, `종료확인`, `빈방`이 라벨+글리프와 함께 표시.
  - `사용중`은 남은분/종료예정 표시, 남은분 음수 없음.
  - `종료확인`은 행동 문구 표시.
  - `빈방`은 즉시 가능 또는 `-` 값 표시.
  - 카드 클릭/상태 클릭이 edit modal, autosave, call ledger grid를 열지 않음.
- Refresh/loading tests:
  - `/rooms/loading.tsx` skeleton 존재.
  - 마지막 갱신/갱신 지연/수동 새로고침 표시.
  - `waitForTimeout()` 없이 Playwright web assertions 사용.

### File Structure Requirements

Expected updated files:

- `src/app/(erp)/rooms/page.tsx`
- `src/components/domain/room-status-card.tsx` only if guidance behavior needs a small prop/label refinement
- `src/app/(erp)/live/page.tsx` and `src/app/(erp)/live/live-refresh-controller.tsx` only if refresh controller is moved/renamed for route-neutral reuse
- `src/lib/authorization.ts` only if tests reveal missing role contract; do not broaden waiter permissions
- `src/lib/navigation.ts` only if tests reveal missing menu contract; do not show unauthorized disabled items
- `package.json`
- `_bmad-output/project-context.md`
- `docs/modules/rooms.md`
- `src/modules/rooms/README.md`
- optionally `docs/modules/calls.md`

Expected new files:

- `src/app/(erp)/rooms/loading.tsx`
- `src/components/domain/room-status-refresh-controller.tsx` or equivalent route-neutral refresh component
- `scripts/validate-story-3-3.mjs`
- `tests/e2e/story-3-3-rooms-waiter-guidance.spec.ts`

Avoid unless explicitly justified:

- New Prisma tables or migrations.
- New chart/KPI/dashboard code.
- New React Query/SWR/global state dependency.
- New Server Actions or write API for `/rooms`.
- Changes to `/tv` fullscreen route beyond shared component compatibility; Story 3.4 owns it.

## Project Structure Notes

- Alignment: `/rooms` belongs under `src/app/(erp)/rooms`, shared card/refresh UI under `src/components/domain`, domain reads under `src/modules/rooms`, route-neutral date helpers under `src/lib`.
- Detected variance: `LiveRefreshController` is currently named for `/live` but its behavior is route-neutral. Prefer extracting/renaming rather than duplicating an identical `/rooms` controller.
- Detected risk: `RoomStatusCard` currently uses "결제·확인 필요" for `종료확인`, while UX Flow 4 suggests 웨이터 안내 문구 "청소/입실 안내 대기". If changing this, preserve Story 3.2 acceptance and test expectations or make the action label variant explicit.
- Existing dirty worktree note: `_bmad-output/story-automator/orchestration-1-20260607-165702.md` has unrelated local modifications. Do not revert or rewrite it for this story unless the user asks.

### References

- `_bmad-output/planning-artifacts/epics.md` - Epic 3, Story 3.3, FR16-FR18
- `_bmad-output/planning-artifacts/prds/prd-vietnam_massage-2026-06-07/prd.md` - UJ-3, FR-16~FR-19, FR-34
- `_bmad-output/planning-artifacts/architecture.md` - domain service boundary, rooms DTO + polling route, authorization/loading/stale state
- `_bmad-output/planning-artifacts/ux-designs/ux-vietnam_massage-2026-06-07/DESIGN.md` - locked status tokens, status badge, contrast corrections
- `_bmad-output/planning-artifacts/ux-designs/ux-vietnam_massage-2026-06-07/EXPERIENCE.md` - Flow 4, room status card, stale state, accessibility floor
- `_bmad-output/project-context.md` - Story 3.1/3.2 room status and read-only rules
- `_bmad-output/implementation-artifacts/3-1-객실-상태-dto와-최신-활성-콜-계산.md`
- `_bmad-output/implementation-artifacts/3-2-첫-화면-실시간-객실-콜-현황.md`
- `src/app/(erp)/rooms/page.tsx`
- `src/app/(erp)/live/page.tsx`
- `src/app/(erp)/live/live-refresh-controller.tsx`
- `src/app/(erp)/live/loading.tsx`
- `src/components/domain/room-status-card.tsx`
- `src/components/domain/status-badge.tsx`
- `src/modules/rooms/dtos.ts`
- `src/modules/rooms/room-status-service.ts`
- `src/lib/authorization.ts`
- `src/lib/navigation.ts`
- `src/components/domain/role-aware-sidebar.tsx`
- Next.js `useRouter`: https://nextjs.org/docs/app/api-reference/functions/use-router
- Next.js Fetching Data / loading states: https://nextjs.org/docs/app/getting-started/fetching-data
- Playwright locators/assertions: https://playwright.dev/docs/locators, https://playwright.dev/docs/test-assertions

## Dev Agent Record

### Agent Model Used

GPT-5 Codex

### Debug Log References

- 2026-06-09: `node scripts/validate-story-3-3.mjs` RED 확인. `/rooms` placeholder, loading, 공용 refresh, docs 누락으로 실패함을 확인했다.
- 2026-06-09: `node scripts/validate-story-3-3.mjs` 통과.
- 2026-06-09: `npm run test:unit` 통과. 현재 npm glob 동작상 `src/lib/operating-date.test.ts` 1개 suite, 3개 test가 실행됐다.
- 2026-06-09: `npm run lint` 최초 실행은 Story 3.2 validator가 기존 `live-refresh-controller.tsx` 위치를 요구해 실패했다. Story 3.3의 route-neutral refresh 추출에 맞춰 `scripts/validate-story-3-2.mjs`를 공용 컴포넌트 기준으로 갱신했다.
- 2026-06-09: `npm run lint` 재실행 통과. Story 1.1~3.3 validators 모두 통과했다.
- 2026-06-09: `npm run test:e2e -- tests/e2e/story-3-3-rooms-waiter-guidance.spec.ts` 실행 시 Next dev server listen이 sandbox에서 `Error: listen EPERM: operation not permitted 127.0.0.1:3000`로 차단되어 E2E를 완료하지 못했다.
- 2026-06-09: `npx tsc --noEmit`는 기존 저장소 타입 오류로 실패했다. Story 3.3 관련 파일만 필터링한 확인에서는 신규 오류가 없었다.
- 2026-06-09: REVIEW: `tests/e2e/story-3-3-rooms-waiter-guidance.spec.ts`에서 `@node-rs/argon2` ambient const enum 직접 참조를 제거해 Story 3.3 관련 `tsc --noEmit` 필터 오류를 해소했다.
- 2026-06-09: REVIEW: `/rooms` 카드 grid와 loading skeleton을 `grid-cols-1 sm:grid-cols-2 xl:grid-cols-4`로 보강하고 Story 3.3 validator guard를 갱신했다.
- 2026-06-09: REVIEW: `node scripts/validate-story-3-3.mjs`, `npm run test:unit`, `npm run lint` 재통과. `npm run test:e2e -- tests/e2e/story-3-3-rooms-waiter-guidance.spec.ts`는 sandbox `listen EPERM 127.0.0.1:3000`으로 계속 차단됨.
- 2026-06-09: REVIEW RERUN: Story 3.3 E2E fixture의 `Employee`/`CodeItem` seed가 shared unique sortOrder를 고정값으로 가정해 재실행 또는 기존 E2E 데이터와 충돌할 수 있음을 확인하고, fixture-local sortOrder 예약 helper로 idempotent하게 수정했다.
- 2026-06-09: REVIEW RERUN: `node scripts/validate-story-3-3.mjs`, `npm run test:unit`, `npm run lint` 통과. `npm run test:e2e -- tests/e2e/story-3-3-rooms-waiter-guidance.spec.ts` 재실행은 seed setup 전 Next dev server `listen EPERM 127.0.0.1:3000`으로 차단됐다.
- 2026-06-09: FINAL VERIFY: `node scripts/validate-story-3-3.mjs`, `npm run test:unit`, `npm run lint` 통과. `npm run test:e2e -- tests/e2e/story-3-3-rooms-waiter-guidance.spec.ts` 최종 재실행은 Playwright worker까지 진입했지만 Prisma가 E2E DB에 연결하지 못해 `ECONNREFUSED`로 차단됐다.

### Implementation Plan

- `/rooms`는 Server Component에서 `requireRouteAccess("/rooms")`, 운영월/날짜 선택, `listRoomStatuses()` 조회만 수행하게 구성했다.
- `/live` 전용 refresh controller를 `RoomStatusRefreshController`로 공용화해 `/live`와 `/rooms`가 같은 15초 `router.refresh()`/지연 표시 affordance를 사용하게 했다.
- `/rooms`는 `RoomStatusDto`와 `RoomStatusCard`를 그대로 렌더링하고, 콜 원장 grid/autosave/지출/정산/월마감 mutation UI를 추가하지 않았다.
- Story 3.3 validator와 E2E spec을 추가하고, docs/project-context에 `/rooms` 읽기 전용 및 안내 문구 source-of-truth 계약을 동기화했다.

### Completion Notes List

- Ultimate context engine analysis completed - comprehensive developer guide created.
- `/rooms` placeholder를 실제 객실 현황 화면으로 교체했다. 운영월/조회날짜 조건, 11개 객실 카드, 운영월 없음 안내, 관리자 전용 운영월 관리 링크를 포함한다.
- 웨이터/read-only 객실 현황 화면이 `listRoomStatuses()`와 `RoomStatusCard`를 재사용하도록 구현했다. 상태별 안내 문구, 남은분, 종료예정, 빈방 `-` 표시는 DTO/card source of truth에 맡긴다.
- `RoomStatusRefreshController`를 공용 컴포넌트로 추가해 `/live`와 `/rooms`가 같은 마지막 갱신/갱신 중/갱신 지연/수동 새로고침 동작을 사용한다.
- Story 3.3 E2E spec과 validator를 추가하고 `npm run lint` chain에 연결했다.
- E2E는 dev server listen `EPERM` sandbox 차단으로 실행 완료하지 못했다. 정적 validator, unit test, lint는 통과했다.

### File List

- `_bmad-output/implementation-artifacts/3-3-객실-현황-화면과-웨이터-안내-문구.md`
- `_bmad-output/implementation-artifacts/sprint-status.yaml`
- `_bmad-output/project-context.md`
- `docs/modules/calls.md`
- `docs/modules/rooms.md`
- `package.json`
- `scripts/validate-story-3-2.mjs`
- `scripts/validate-story-3-3.mjs`
- `src/app/(erp)/live/live-refresh-controller.tsx` (deleted)
- `src/app/(erp)/live/page.tsx`
- `src/app/(erp)/rooms/loading.tsx`
- `src/app/(erp)/rooms/page.tsx`
- `src/components/domain/room-status-refresh-controller.tsx`
- `src/modules/rooms/README.md`
- `tests/e2e/story-3-3-rooms-waiter-guidance.spec.ts`

### Senior Developer Review (AI)

Reviewer: noah  
Date: 2026-06-09  
Outcome: Approved with sandbox-limited E2E

Findings:

- [MEDIUM] `tests/e2e/story-3-3-rooms-waiter-guidance.spec.ts` directly referenced `Algorithm.Argon2id` from `@node-rs/argon2`, which creates a Story 3.3-specific `isolatedModules` type error in `npx tsc --noEmit`. Fixed by removing the const enum import and using the runtime numeric algorithm value in the test fixture.
- [MEDIUM] `/rooms` and `/rooms/loading` used a fixed `grid-cols-4` layout. On narrower viewports, the 11 read-only cards could collapse below usable width. Fixed with `grid-cols-1 sm:grid-cols-2 xl:grid-cols-4` and updated the Story 3.3 validator to enforce the responsive grid contract.
- [MEDIUM] Task 8 marked the focused E2E command complete even though the recorded execution was blocked by sandbox `listen EPERM 127.0.0.1:3000`. Fixed by leaving that command unchecked and preserving the blocked validation note.
- [MEDIUM] `tests/e2e/story-3-3-rooms-waiter-guidance.spec.ts` used fixed Story 3.3 `Employee`/`CodeItem` sortOrder values during fixture upserts. In a reused E2E database, those values can collide with existing rows and fail before the test reaches `/rooms`. Fixed by reserving a free fixture-local sortOrder while preserving existing Story 3.3 fixture rows on rerun.

Review Fix Log:

- Removed the Story 3.3 E2E ambient const enum access and verified no Story 3.3-specific `tsc --noEmit` filter errors remain.
- Updated `/rooms` page/loading grid responsiveness and the Story 3.3 validator guard.
- Re-ran `node scripts/validate-story-3-3.mjs`, `npm run test:unit`, and `npm run lint`; all passed.
- Re-ran the focused Story 3.3 E2E command; it remains blocked by sandbox `listen EPERM 127.0.0.1:3000`.
- Added Story 3.3 fixture-local sortOrder reservation for account employees, therapist, and service status code items; re-ran `node scripts/validate-story-3-3.mjs`, `npm run test:unit`, and `npm run lint`; all passed.
- Re-ran the focused Story 3.3 E2E command after the fixture fix; it remains blocked before seed setup by sandbox `listen EPERM 127.0.0.1:3000`.
- Final orchestrator verification re-ran the focused Story 3.3 E2E command; it reached Playwright worker setup but remains incomplete because the E2E database connection is refused (`ECONNREFUSED`).

### Change Log

- 2026-06-09: Implemented Story 3.3 `/rooms` read-only room status screen, shared refresh controller, loading skeleton, validator/E2E coverage, and docs/project-context sync.
- 2026-06-09: Senior developer review auto-fixes applied for Story 3.3 E2E type safety, responsive room-card grid, validation guard, and blocked E2E record accuracy.
- 2026-06-09: Senior developer review rerun fixed Story 3.3 E2E fixture idempotency for shared employee/code sortOrder constraints and updated validation records.
