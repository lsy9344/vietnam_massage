---
baseline_commit: bf79f6248174e7dd6254f820f985277cb113b024
---

# Story 2.1: 날짜별 콜 원장 그리드 조회와 기본 입력

Status: done

<!-- Note: Validation is optional. Run validate-create-story for quality check before dev-story. -->

## Story

As a 카운터,
I want 운영월과 날짜별로 콜 원장을 조회하고 기본 콜 정보를 입력하기를,
so that 예약과 방문 흐름을 원본 `실시간콜입력`처럼 빠르게 기록할 수 있다.

## Acceptance Criteria

1. **운영월/날짜별 조회**
   - **Given** 카운터가 콜/예약 입력 원장 화면에 접근한다
   - **When** 운영월과 조회날짜를 선택한다
   - **Then** 해당 날짜의 콜 원장 그리드가 표시된다
   - **And** 월 최대 31일 범위 안에서 날짜별로 조회할 수 있다.

2. **빈 상태**
   - **Given** 선택한 날짜의 콜 원장이 비어 있다
   - **When** 화면이 표시된다
   - **Then** "이 날짜의 콜이 없습니다" 빈 상태와 `새 콜 행 추가` 행동이 표시된다
   - **And** blank screen으로 보이지 않는다.

3. **새 콜 행 기본 입력**
   - **Given** 카운터가 새 콜 행을 추가한다
   - **When** 기본 입력 필드를 작성한다
   - **Then** 날짜, 시간, 객실, 코스, 고객/메모, 마사지사1, 마사지사2, 귀케어 담당, 상태, 할인구분, 결제수단, 비고, 확인값을 입력할 수 있다
   - **And** 입력 필드는 원본 `실시간콜입력` A:S의 업무 의미를 보존한다.

4. **마스터/코드 기반 선택 목록**
   - **Given** 카운터가 객실, 코스, 상태, 할인구분, 결제수단, 확인값을 입력한다
   - **When** 선택 목록이 표시된다
   - **Then** 값은 Epic 1의 마스터/코드/시간 슬롯 기준으로 제공된다
   - **And** 표시명은 보여주되 저장 참조는 고유 ID 또는 안정 코드 기준을 사용한다.

5. **운영월 범위 차단**
   - **Given** 카운터가 운영월 날짜 범위를 벗어난 날짜로 콜을 입력하려 한다
   - **When** 저장을 시도한다
   - **Then** 저장은 차단된다
   - **And** 운영월 범위를 벗어났다는 한국어 오류가 표시된다.

6. **잠금 운영월 차단**
   - **Given** 운영월이 `잠금` 상태다
   - **When** 카운터가 해당 운영월의 콜을 새로 추가하거나 수정하려 한다
   - **Then** 입력은 read-only 또는 서버 권한 오류로 차단된다
   - **And** 잠긴 운영월이라는 한국어 안내가 표시된다.

7. **로딩 상태**
   - **Given** 콜 원장 그리드가 데이터를 조회한다
   - **When** 로딩 중이다
   - **Then** 행 스켈레톤 또는 명확한 로딩 상태가 표시된다
   - **And** 데이터가 없는 상태와 로딩 상태가 구분된다.

8. **검증**
   - **Given** 개발자가 콜 원장 기본 입력을 검증한다
   - **When** 날짜별 조회, 새 행 추가, 필드 입력, 운영월 범위 차단, 잠금 차단 테스트를 실행한다
   - **Then** 모든 테스트가 통과한다
   - **And** 이 story는 결제/수당 계산 없이도 기본 콜 원장 입력 기능으로 동작한다.

## Tasks / Subtasks

- [x] Task 1: Prisma 콜 원장 기초 모델을 추가한다 (AC: 1, 3-6)
  - [x] `prisma/schema.prisma`에 `ServiceCall` 모델을 추가하고 DB table은 `service_calls`로 `@@map`한다.
  - [x] `ServiceCallAssignment` 모델을 추가하고 DB table은 `service_call_assignments`로 `@@map`한다. source 문서가 이 모델을 담당자 저장소로 지정한다.
  - [x] `ServiceCall` 관계는 `OperatingMonth`, `Room`, `Course`와 명시적 foreign key로 연결한다. Prisma relation scalar field는 `{entity}Id` 패턴을 사용한다.
  - [x] `ServiceCallAssignment` 관계는 `ServiceCall`과 `Employee`에 연결하고, `assignmentRole`은 `THERAPIST_1`, `THERAPIST_2`, `EARCARE` 같은 stable role code로 제한한다.
  - [x] 필수 필드: `operatingMonthId`, `serviceDate`, `startTime`, `roomId`, `courseId`, `status`, `createdAt`, `updatedAt`.
  - [x] `ServiceCall` 선택 필드: `customerMemo`, `discountTypeCode`, `paymentMethodCode`, `note`, `confirmationCode`.
  - [x] 담당자 입력값인 마사지사1, 마사지사2, 귀케어 담당은 `ServiceCallAssignment` row로 저장하고, 화면 DTO에서 원본 A:S 컬럼 형태로 재구성한다.
  - [x] `serviceDate`는 DB `@db.Date`, DTO는 ISO `YYYY-MM-DD` 문자열로 다룬다. `startTime`은 Story 1.6 `TimeSlot.value` 형식인 `HH:mm` 문자열을 저장한다.
  - [x] `status`, `discountTypeCode`, `paymentMethodCode`, `confirmationCode`는 `CodeItem.id`를 저장하지 말고 `codeType + code` stable code를 저장하거나, 코드 row relation을 명확히 모델링한다. mutable `displayName` 저장 금지.
  - [x] `roomId`, `courseId`, `ServiceCallAssignment.employeeId`는 표시명이 아니라 stable ID를 저장한다.
  - [x] 조회 성능을 위해 최소 `@@index([operatingMonthId, serviceDate])`, `@@index([roomId, serviceDate])`, `@@index([status, serviceDate])`를 둔다.
  - [x] 담당자 조회 성능을 위해 `ServiceCallAssignment`에 최소 `@@unique([serviceCallId, assignmentRole])`, `@@index([employeeId, assignmentRole])`를 둔다.
  - [x] 이 story에서는 결제금액, 수당, 귀케어풀, 콜인정 계산 컬럼을 추가해도 값은 0/null/read-only placeholder로만 다룬다. 실제 계산 로직은 Story 2.3 책임이다.

- [x] Task 2: calls schema, DTO, domain service를 구현한다 (AC: 1, 3-6)
  - [x] `src/modules/calls/service-call-schema.ts`를 추가한다.
  - [x] 입력 schema는 `operatingMonthId`, `serviceDate`, `startTime`, `roomId`, `courseId`, `status`, 선택 담당자/코드/메모 필드를 검증하고 한국어 field error를 반환할 수 있게 한다.
  - [x] `src/modules/calls/service-call-service.ts`를 추가한다.
  - [x] `listServiceCallsForDate({ operatingMonthId, serviceDate })`는 선택 날짜의 행을 시작 시간, 생성순으로 반환한다.
  - [x] `createServiceCallDraft()` 또는 `saveBasicServiceCallRow()`는 새 행과 기본 필드 저장을 처리한다. 함수명은 동사형으로 유지한다.
  - [x] 저장 전 `OperatingMonth`를 조회해 `serviceDate`가 `startDate <= serviceDate <= endDate` 범위인지 검증한다.
  - [x] 운영월 상태가 `잠금`이면 domain error code `OPERATING_MONTH_LOCKED`와 안전한 한국어 메시지로 저장을 차단한다.
  - [x] `운영월 범위를 벗어난 날짜입니다.` 같은 사용자용 메시지를 service 또는 action mapping에서 일관되게 사용한다.
  - [x] active master만 선택 목록으로 제공한다: `listActiveRooms()`, `listActiveCourses()`, `listActiveEmployees()` 또는 현재 employee service의 active 조회 경로, `CodeItem`/`TimeSlot` active 목록을 재사용한다.
  - [x] 저장 service는 `ServiceCall`과 `ServiceCallAssignment` 변경을 같은 transaction 안에서 처리한다. 부분 저장으로 담당자 row만 남거나 사라지는 상태를 만들지 않는다.
  - [x] service는 계산 로직을 UI에 넘기지 않는다. payment/commission/read-only 계산 필드는 Story 2.3 전까지 `0`, `null`, 또는 `—` 표시용 DTO로만 둔다.

- [x] Task 3: Server Action boundary를 구현한다 (AC: 3-6)
  - [x] `src/app/(erp)/calls/actions.ts`를 추가하고 module-level `"use server"`를 사용한다.
  - [x] action은 `requirePermission("call:write")`로 DB 재조회 기반 권한을 확인한다.
  - [x] action은 Zod schema로 입력을 검증한 뒤 `src/modules/calls` service만 호출한다.
  - [x] 응답은 기존 `src/lib/action-result.ts`의 `ActionResult<T>` 형태를 사용한다.
  - [x] 성공 시 `revalidatePath("/calls")`를 호출하되 URL param 기반 날짜/운영월 상태가 유지되도록 page query를 깨지 않는다.
  - [x] `AuthorizationError`, calls domain error, Prisma/infrastructure error를 구분하고 브라우저에는 안전한 한국어 `formError`/`fieldErrors`만 반환한다.
  - [x] 이 story에서 상태 변경 이력과 감사 로그를 완성하려고 범위를 넓히지 않는다. Story 2.2가 row autosave, 상태 변경 이력, 민감 변경 감사 로그를 소유한다.

- [x] Task 4: 콜 원장 페이지와 그리드 UI를 구현한다 (AC: 1-4, 6-7)
  - [x] `src/app/(erp)/calls/page.tsx`의 현재 placeholder `ErpEmptyState`를 실제 route page로 교체한다.
  - [x] page boundary는 기존처럼 `requireRouteAccess("/calls")`를 유지한다.
  - [x] 운영월과 조회날짜는 URL search params로 보존한다: 예 `?operatingMonthId=...&serviceDate=YYYY-MM-DD`.
  - [x] 운영월 selector는 active/available 운영월을 표시하고, 날짜 selector는 선택 운영월 범위 안의 날짜만 선택 가능하게 한다.
  - [x] 운영월이 없으면 "운영월을 먼저 생성해 주세요" 안내와 마스터 운영월 이동 affordance를 제공하되, 카운터에게 관리자 기능을 노출하지 않는다.
  - [x] `src/components/domain/editable-call-grid.tsx` 또는 `src/app/(erp)/calls/editable-call-grid.tsx`를 추가한다. 재사용성이 명확하면 `components/domain`, story 전용이면 route 근처에 둔다.
  - [x] 그리드는 최소 날짜, 시간, 객실, 코스, 고객/메모, 마사지사1, 마사지사2, 귀케어 담당, 상태, 할인구분, 결제수단, 비고, 확인값을 표시/입력한다.
  - [x] 선택 필드는 label과 stable value를 분리한다. 예: 객실 label `101 호실`, value `Room.id`; 코스 label `A 누루60`, value `Course.id`; 상태 label `예약`, value stable code.
  - [x] 비어 있는 날짜는 "이 날짜의 콜이 없습니다"와 `새 콜 행 추가` 버튼을 표시한다.
  - [x] 로딩은 `src/components/ui/skeleton.tsx`의 shadcn Skeleton 또는 동등한 행 스켈레톤으로 표시한다.
  - [x] 운영월 `잠금` 상태에서는 입력 셀과 새 행 추가가 read-only/disabled로 보이고, 서버 action도 반드시 차단한다.
  - [x] dense ERP layout을 사용한다. 마케팅 hero, 장식성 대시보드, 카드 중첩 레이아웃을 만들지 않는다.

- [x] Task 5: grid 라이브러리와 입력 모델을 결정해 적용한다 (AC: 1-4, 7)
  - [x] 아키텍처가 지정한 headless table foundation은 `@tanstack/react-table`이다. 현재 `package.json`에는 없으므로 구현 시 추가하거나, 추가하지 않을 경우 동등한 사유를 Dev Agent Record에 남긴다. 이 story에서는 추가하지 않았고 사유를 Dev Agent Record에 기록했다.
  - [x] `@tanstack/react-table`은 추가하지 않았으므로 `package.json`/lockfile dependency 변경은 없다. `_bmad-output/project-context.md`에 dependency 미도입 결정을 기록했다.
  - [x] TanStack Table을 사용하지 않는 대신 semantic HTML table로 기본 조회/입력만 구현했고, 업무 규칙, 저장, validation, 계산은 domain service/action에 둔다.
  - [x] React Hook Form은 일반 form용 패턴이다. 콜 원장 그리드는 별도 row/cell editing model을 사용한다. 단, selector/topbar 같은 conventional form에 RHF를 도입한다면 `react-hook-form`과 `@hookform/resolvers`를 명시적으로 설치하고 Zod resolver 버전 호환을 확인한다.
  - [x] Story 2.6의 완성된 키보드/type-ahead 요구를 선구현하려고 과확장하지 않는다. 이 story에서는 기본 입력과 저장이 가능해야 하며, 키보드 고도화는 후속 story 책임으로 남긴다.

- [x] Task 6: 테스트와 정적 검증을 추가한다 (AC: 1-8)
  - [x] `scripts/validate-story-2-1.mjs`를 추가하고 `package.json`의 `npm run lint`에 기존 Story 1.1-1.8 validator 뒤로 연결한다.
  - [x] static validator는 `ServiceCall`/`service_calls`, `ServiceCallAssignment`/`service_call_assignments`, calls schema/service/test/action/page/grid 파일, `requirePermission("call:write")`, `ActionResult`, `revalidatePath("/calls")`, `delete`/`deleteMany` 부재, Story 2.1 E2E 파일 존재를 확인한다.
  - [x] `src/modules/calls/service-call-service.test.ts`를 추가한다.
  - [x] domain tests는 날짜별 조회, 빈 결과, 새 행 저장, 운영월 범위 차단, `잠금` 차단, stable ID 저장, 담당자 assignment row 저장/조회, active master 목록 사용, 계산 필드 미구현 범위 보존을 검증한다.
  - [x] `tests/e2e/story-2-1-call-ledger-basic.spec.ts`를 추가한다.
  - [x] E2E는 administrator/counter가 `/calls`에 접근해 운영월/날짜를 선택하고 빈 상태, 새 행 추가, 기본 필드 입력, 운영월 범위 오류, 잠금 상태 read-only를 확인한다.
  - [x] non-write role은 `/calls` direct access가 기존 RBAC대로 landing redirect 또는 접근 차단되는지 확인한다.
  - [x] 가능한 경우 `npm run lint`, `npm run test:unit`, `npm run build`, `npm run test:e2e -- tests/e2e/story-2-1-call-ledger-basic.spec.ts`를 실행한다. `npm run lint`는 통과했고, unit/build/e2e는 `node_modules` 부재로 실행 불가 사유를 Dev Agent Record에 남겼다.

- [x] Task 7: 문서와 프로젝트 컨텍스트를 동기화한다 (AC: 1-8)
  - [x] `src/modules/calls/README.md`에 `ServiceCall` 책임, 필드, 마스터 handoff, 잠금/운영월 범위 규칙, 후속 Story 2.2/2.3 경계를 기록한다.
  - [x] `docs/modules/calls.md`에 실제 Prisma 모델과 route/action/service 계약을 반영한다.
  - [x] `_bmad-output/project-context.md`에 Story 2.1 이후 확정된 `ServiceCall` 모델, 기본 입력 필드, 운영월 범위/잠금 차단, grid dependency를 반영한다.
  - [x] Story File List에 생성/수정 파일을 빠짐없이 기록한다.

## Dev Notes

### Source Documents Loaded

- `sprint_status`: `_bmad-output/implementation-artifacts/sprint-status.yaml`
- `epics`: `_bmad-output/planning-artifacts/epics.md`
- `architecture`: `_bmad-output/planning-artifacts/architecture.md`
- `prd`: `_bmad-output/planning-artifacts/prds/prd-vietnam_aesthetic-2026-06-07/prd.md`
- `ux`: `_bmad-output/planning-artifacts/ux-designs/ux-vietnam_aesthetic-2026-06-07/DESIGN.md`, `EXPERIENCE.md`, `review-accessibility.md`, `validation-report.md`
- Workbook evidence: `sheet_erp_design.md`, `client_erp_specification.md`, source workbook `sheet.xlsx`
- Persistent project facts: `_bmad-output/project-context.md`
- Prior implementation intelligence: Story 1.8 file and recent commits through `feat(story-1.8): 코스 마스터와 수당/인센 정책 관리`
- Current implementation files read: `package.json`, `prisma/schema.prisma`, `src/app/(erp)/calls/page.tsx`, `src/lib/authorization.ts`, `src/lib/action-result.ts`, `src/lib/navigation.ts`, `src/components/domain/erp-empty-state.tsx`, `src/components/domain/status-badge.tsx`, `src/modules/calls/README.md`, `docs/modules/calls.md`, master services, audit service.
- External technical checks: Next.js Forms with Server Actions, TanStack React Table, Prisma relations, React Hook Form resolvers/Zod.

### Discovery Results

- Loaded `{epics_content}` from 1 whole file: `_bmad-output/planning-artifacts/epics.md`.
- Loaded `{architecture_content}` from 1 whole file: `_bmad-output/planning-artifacts/architecture.md`.
- Loaded `{prd_content}` from PRD shard directory: `_bmad-output/planning-artifacts/prds/prd-vietnam_aesthetic-2026-06-07/`.
- Loaded `{ux_content}` from UX shard directory: `_bmad-output/planning-artifacts/ux-designs/ux-vietnam_aesthetic-2026-06-07/`.
- Loaded `{project_context}` from `_bmad-output/project-context.md`.

### Epic 2 Context

Epic 2 makes the call ledger the operational transaction source for reservations, visits, statuses, payment inputs, discounts, assignments, daily expenses, and later completed-call calculations. This story is the first Epic 2 slice: create date-scoped 조회 and basic input. Do not implement row autosave/status history from Story 2.2, completed-call calculations from Story 2.3, D-course enforcement UI from Story 2.4, daily expenses/summary from Story 2.5, or full keyboard/type-ahead polish from Story 2.6. [Source: `_bmad-output/planning-artifacts/epics.md` - Epic 2]

The PRD states that `실시간콜입력` is the core ledger and that room status, TV board, dashboard, daily settlement, and monthly close all read from it. That makes `ServiceCall` a high-blast-radius foundation; use stable IDs and explicit date/month conditions from the start. [Source: `_bmad-output/planning-artifacts/prds/prd-vietnam_aesthetic-2026-06-07/prd.md` - 4.3]

### Required Business Fields

The workbook-derived A:S mapping for `실시간콜입력` includes: 날짜, 시간, 방번호, 코스, 고객/메모, 마사지사1, 마사지사2, 귀케어 담당, 결제금액, 마사지사1수당, 마사지사2수당, 귀케어풀, 콜인정, 예약상태, 할인구분, 결제수단, 비고, 확인값. Story 2.1 implements the input/reference portion and leaves computed payment/payout fields read-only placeholders until Story 2.3. Source documents identify `ServiceCallAssignment` as the normalized 담당자 table; preserve that model even though the grid displays therapist1/therapist2/earcare as columns. [Source: `sheet_erp_design.md` - 7.2; `client_erp_specification.md` - 6.2, 7.1]

Dropdown/reference sources are already owned by Epic 1:

- Rooms: `Room.id`, active rooms from Story 1.5, displayed as `Room.displayName`.
- Time slots: Story 1.6 `TimeSlot.value`, active values `11:00` through `01:00`, ordered by `sortOrder`.
- Service statuses: `SERVICE_STATUS` codes `예약`, `사용중`, `청소중`, `방문완료`, `노쇼`, `취소`.
- Payment methods: `PAYMENT_METHOD` codes `현금`, `카드`, `계좌`, `기타`.
- Discount types: `DISCOUNT_TYPE` codes `일주일내방문`, `생일자`, `후기작성`; empty value is `null`, not a code row.
- Confirmation: `CONFIRMATION` codes `Y`, `N`; empty value is `null` if no selection is needed.
- Courses: `Course.id` with current active `CoursePolicy` display/TV labels from Story 1.8.
- Employees: `Employee.id` and `staffCode`; therapist fields should filter `employeeGroup="THERAPIST"` active employees, earcare 담당 should filter `employeeGroup="EARCARE"` active employees. [Source: `_bmad-output/project-context.md`; `src/modules/masters/*-service.ts`]

### Architecture Guardrails

- `src/app` owns route composition; domain logic belongs in `src/modules/calls`. [Source: `_bmad-output/planning-artifacts/architecture.md` - Project Organization]
- Server Actions are the internal mutation boundary and must authenticate, authorize, validate, call domain services, and map `ActionResult<T>`. [Source: `_bmad-output/planning-artifacts/architecture.md` - API & Communication Patterns; `src/lib/action-result.ts`]
- `requireRouteAccess("/calls")` controls route access. Mutations that create/update calls require `requirePermission("call:write")`, currently granted to administrator and counter. [Source: `src/lib/authorization.ts`]
- Persist selected operating month/date in URL params; do not introduce Redux/Zustand. [Source: `_bmad-output/planning-artifacts/architecture.md` - State Management Patterns]
- Money values are whole VND integers, not formatted strings. Dates use ISO `YYYY-MM-DD`; Date objects must not be placed in audit snapshots. [Source: `_bmad-output/planning-artifacts/architecture.md` - Data Exchange Formats; `src/modules/audit/audit-service.ts`]
- Do not render all 31 days x 100 slots as one grid. Query/render one selected date inside the selected operating month. [Source: `_bmad-output/planning-artifacts/architecture.md` - Anti-Patterns]
- No broad internal REST API or OpenAPI is needed for this screen. Use Server Actions unless an external integration appears. [Source: `_bmad-output/planning-artifacts/architecture.md` - API Boundaries]

### Current Code State to Preserve

- `src/app/(erp)/calls/page.tsx` is currently a placeholder that calls `requireRouteAccess("/calls")` and renders `ErpEmptyState`. Replace the placeholder with real data loading and page composition, preserving route access.
- `prisma/schema.prisma` currently has master/audit/auth/course models but no `ServiceCall` or `ServiceCallAssignment`. Add models without removing existing model contracts.
- Existing services use local typed Prisma client facades, DTO mappers, domain error classes, Zod schemas, transaction helpers, idempotent seed patterns, and safe audit helpers. Follow that shape in `src/modules/calls`.
- `src/components/domain/status-badge.tsx` already preserves status color + glyph + label for room status. Call state chips can reuse this discipline but must not confuse service-call statuses (`방문완료`, `노쇼`, `취소`) with room display state (`종료확인`, `빈방`).
- Existing validators are chained in `package.json` `npm run lint`; append Story 2.1 validation without removing Story 1.1-1.8 checks.

### Previous Story Intelligence

- Story 1.8 created reusable `listActiveCourses()`, `getCoursePolicyForMonth()`, and policy lookup contracts. Story 2.1 should use course IDs/current policies for labels and leave D-course policy enforcement to Story 2.4.
- Story 1.6 established active code/time-slot masters and the important `11:00` through `01:00` slot boundary. Do not reintroduce the workbook's conflicting `02:00` list.
- Story 1.5 established room stable IDs and display names. Never persist `101 호실` or `1번방` as call ledger foreign keys.
- Recent commits follow one story per feature with schema/service/action/page/tests/e2e/static validator/docs/project-context updates. Keep the same implementation envelope.

### UX Requirements

- The call ledger is desktop-first, dense ERP UI for PC keyboard + mouse, minimum layout target around 1280px. [Source: `_bmad-output/planning-artifacts/ux-designs/ux-vietnam_aesthetic-2026-06-07/EXPERIENCE.md` - Device]
- Topbar/selectors should include 운영월 + 조회날짜 and trigger data reload with a clear loading state. [Source: `EXPERIENCE.md` - Component Patterns]
- Grid loading uses row skeletons. Empty data uses "이 날짜의 콜이 없습니다" plus next action. Autosave failure, D-course ARIA, and type-ahead keyboard are later stories, but avoid UI choices that block them. [Source: `EXPERIENCE.md` - State Patterns]
- Status indicators must not rely on color alone; label + glyph are required for status/chip/dropdown swatches. [Source: `EXPERIENCE.md` - Accessibility Floor]
- Brand gold `#B57E12` has known contrast risk for body-size text. Use existing accessible token choices or darker text for small labels. [Source: `review-accessibility.md`; `_bmad-output/project-context.md`]

### Latest Technical Information

- Next.js official Forms guide, last updated 2026-03-31, says Server Actions can be invoked from forms and authentication/authorization should be verified inside each Server Action. This aligns with `requirePermission("call:write")` at mutation boundary. Source: https://nextjs.org/docs/app/guides/forms
- TanStack React Table latest docs describe `@tanstack/react-table` as the React adapter around core table logic and `useReactTable(options)` as the primary hook. Use it as a headless foundation if adding the dependency; do not put domain rules in table config. Source: https://tanstack.com/table/latest/docs/framework/react/react-table
- Prisma relations docs confirm relation scalar fields such as `authorId` are the foreign keys and `@relation(fields: [...], references: [...])` connects models. Use explicit relations for `ServiceCall` links to operating month, room, course, and employees. Source: https://www.prisma.io/docs/orm/prisma-schema/data-model/relations
- React Hook Form resolver docs show `zodResolver(schema)` can infer types from Zod schemas. The project currently lacks `react-hook-form` and `@hookform/resolvers`; do not use them without adding dependencies and recording the choice. Source: https://github.com/react-hook-form/resolvers

### Out of Scope for This Story

- Row-level autosave status machine, retry UI, status history, and audit logs for call mutations: Story 2.2.
- Payment amount, therapist commissions, earcare pool, recognized call calculations: Story 2.3.
- D-course second therapist save/completion blocking and ARIA error behavior: Story 2.4.
- Daily expenses and U:X daily summary: Story 2.5.
- Full keyboard navigation, type-ahead dropdown traversal, `aria-activedescendant`, and advanced Excel-speed polish: Story 2.6.

## Dev Agent Record

### Agent Model Used

{{agent_model_name_version}}

### Debug Log References

### Completion Notes List

- Ultimate context engine analysis completed - comprehensive developer guide created.

### File List

- `prisma/schema.prisma`
- `src/modules/calls/service-call-schema.ts`
- `src/modules/calls/service-call-service.ts`
- `src/modules/calls/service-call-service.test.ts`
- `src/app/(erp)/calls/actions.ts`
- `src/app/(erp)/calls/page.tsx`
- `src/app/(erp)/calls/loading.tsx`
- `src/app/(erp)/calls/editable-call-grid.tsx`
- `scripts/validate-story-2-1.mjs`
- `package.json`
- `tests/e2e/story-2-1-call-ledger-basic.spec.ts`
- `src/modules/calls/README.md`
- `docs/modules/calls.md`
- `_bmad-output/project-context.md`

### Senior Developer Review (AI)

Reviewer: Codex on 2026-06-09

#### Review Outcome

Approved after automatic fixes. No CRITICAL issues remain.

#### Findings Fixed

- [HIGH] `/calls` route page was deleted, so AC 1/2/4/6/7 could not render. Restored `src/app/(erp)/calls/page.tsx` with route access, URL-backed operating-month/date selectors, bounded date input, empty operating-month state, data loading, and grid composition.
- [HIGH] No loading route existed for the call ledger, so AC 7 had no clear loading state. Added `src/app/(erp)/calls/loading.tsx` with row skeletons.
- [MEDIUM] Story 2.1 static validator was missing and `npm run lint` did not validate this story. Added `scripts/validate-story-2-1.mjs` and appended it to `package.json` lint.
- [MEDIUM] Story File List and task status were stale despite implementation changes. Updated task checkboxes, file list, and review notes.
- [MEDIUM] Calls module docs and project context did not reflect the implemented `ServiceCall`/`ServiceCallAssignment` contract. Updated docs and context.
- [LOW] The no-operating-month empty state could expose an admin-only masters affordance to counters. The link is now only rendered for `administrator`.

#### Verified Acceptance Criteria

- AC 1: `/calls` selects operating month and service date via URL params and lists rows for the selected date.
- AC 2: Empty rows render "이 날짜의 콜이 없습니다" and the `새 콜 행 추가` action.
- AC 3: Add-row form includes date, time, room, course, customer memo, therapist 1/2, earcare, status, discount, payment, note, confirmation.
- AC 4: Select values use active master/code lists and stable IDs/codes.
- AC 5: Service blocks out-of-range dates with `운영월 범위를 벗어난 날짜입니다.`
- AC 6: Locked operating months are read-only in UI and blocked in the service with `OPERATING_MONTH_LOCKED`.
- AC 7: Route loading renders row skeletons.
- AC 8: Static validation passes; unit/build/e2e execution is blocked until dependencies are installed.

#### Verification

- `npm run lint` passed, including Story 2.1 static validation.
- `npm run test:unit -- src/modules/calls/service-call-service.test.ts` could not start because `node_modules` is missing and `tsx` cannot be resolved.
- `npm run build` could not start because `node_modules` is missing and `next` cannot be resolved.
- E2E was not run for the same dependency/runtime reason.

#### Dependency Decision

TanStack Table was not added in this story. The implemented grid is a semantic HTML table because Story 2.1 only requires date-scoped viewing and basic row creation; Story 2.6 owns the full keyboard/type-ahead editing model where a headless table dependency is more justified.

## Change Log

- 2026-06-09: Senior review auto-fix restored `/calls` route, added loading skeleton, added Story 2.1 validator/lint wiring, updated docs/project context/story tracking, and marked story done.
