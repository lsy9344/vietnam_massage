# Shared Types

Reserved for cross-module type contracts.

Examples:

- `YearMonth`
- `DateRange`
- `MoneyAmount`
- `EmployeeId`
- `RoomId`
- `CourseId`

Keep domain-specific types inside their owning module when possible.

