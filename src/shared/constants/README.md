# Shared Constants

Reserved for constants used across more than one module.

Examples:

- attendance status labels
- reservation status labels
- payment method labels
- discount type labels

Prefer module-owned constants when a value is used by only one module.

